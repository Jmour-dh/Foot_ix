import styled from 'styled-components'

const ButtonStyled = styled.button`
    width: 100%;
    padding: 20px 30px;
    font-size: 1.3rem;
    background: linear-gradient(180deg, var(--primary) 40%, var(--blackColor) 90%);
    border-radius: 10px;
    box-shadow: 0 0 10px black;
    font-weight: 600;
    border: 1px rgba(166, 224, 30, 0.815) solid;
    cursor: pointer;
    color: rgb(228, 228, 228);
`

export default ButtonStyled;